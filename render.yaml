services:
  - type: web
    name: raices-backend
    env: native
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: "echo 'No build'"
    startCommand: "php artisan serve --host=0.0.0.0 --port=10000"
    envVars:
      - key: APP_DEBUG
        value: false